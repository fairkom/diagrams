apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - issuer.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml

images:
  - name: jgraph/drawio
    newTag: latest

commonLabels:
  app: drawio
  environment: base